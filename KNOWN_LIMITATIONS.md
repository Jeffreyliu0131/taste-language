# 已知限制与不可宣称事项

## 结论

Taste Language 证明的是：一套明确的照片选择、冻结、拒答、证据回链和编辑规则可以在本地浏览器中运行。

它没有证明用户需要这项能力，也没有证明候选语言或未见照片方向在真实环境中稳定有效。当前证据结论仍是 **Validate first（先验证）**。

## 产品与研究限制

1. 尚无目标用户行为研究、需求强度、使用频率、付费、留存或编辑负担数据。
2. 四组候选语言加一次方向交换只是可操作的反通用描述示例，不是经过验证的 anti-Barnum protocol。
3. 内置复现序列是人工设计的演示路径，不能作为预测准确率、增量价值或个人稳定性的证据。
4. 一次 session 只描述一个固定情境，不能证明跨日期、跨任务、跨照片来源或跨情境稳定。
5. 看过未见照片后，不能回改训练选择并继续把同一批照片称为“未见”。要更改训练输入，必须重新开始。

## 视觉数据限制

1. 运行时只接受 manifest 中登记的 12 张合成空间照片，不是开放图片搜索或 OOD detector。
2. `openness` 与 `density` 是为机制演示编写的显式属性，不是心理测量真值。
3. 训练与未见照片按登记的来源组和近重复组隔离，但没有独立的感知哈希审计、人工近重复复核或跨摄影来源风格控制。
4. 合成照片仍可能共享生成方式带来的摄影风格。未经额外控制，结果只能称为照片视觉偏好，不能升级为空间氛围或真实地点属性。
5. Fixture 图片与 metadata 按 CC0 提供，但这不保证生成内容具有版权、唯一、不侵权或适合特定用途。许可允许的复用与证据边界分开：这些图片是虚构素材，不能支持关于真实地点的事实主张。详见 [资产许可说明](ASSET_LICENSES.md)。

## 模型与统计限制

1. Feature-difference 分值是低自由度、确定性的 demo heuristic，不是概率、置信度、因果效应或统计真相。
2. 0-2 个候选轴只来自固定的二维可见属性 basis，不能代表人的完整审美或人格。
3. 权重、支持、冲突和低差异阈值尚未用真实样本校准。
4. 当前没有 visual embedding，也没有证据证明更复杂模型能优于显式属性 baseline。
5. 运行时没有 LLM 或外部 provider。确定性模板只证明“无需模型也能运行”，不证明未来模型的质量、安全、成本或延迟。

## UI 隐藏与安全限制

1. 未见照片预测和候选语言来源在界面中暂时隐藏，但仍存在于客户端内存和代码中。它是 `UI-concealed`，不是 participant-grade blind isolation。
2. localStorage 不是机密性、防篡改、多用户隔离或原子并发写入边界。
3. Session digest 用于发现意外损坏和不可能状态，不是密码学签名或可信审计日志。
4. 重置只删除应用自己的 localStorage key；它不会删除已下载的 JSON、浏览器缓存、操作系统备份或取证副本。
5. 可选备注由用户主动输入并保存在本地。长度限制和 React 转义不等于真实 PII 检测。
6. 当前 CSP 适合本地静态 demo，不是通过 HTTP header 部署的 production policy。

## 无障碍与兼容性限制

1. 核心任务要求判断图片，无法为盲人提供等价的视觉偏好任务。Alt text 故意保持中性，避免泄漏显式属性。
2. 已覆盖键盘、阶段焦点、390px mobile、等效 200% reflow、reduced motion、forced colors 和自动化无障碍扫描，但没有真实屏幕阅读器用户研究。
3. 浏览器验证以 Chromium 为主，未完成 Firefox 与 WebKit 矩阵。
4. 运行时哈希依赖 Web Crypto，需要现代浏览器。

## 不可宣称事项

不得据此宣称：

- 用户需求、市场价值或产品方向已经验证；
- 可以判断真实咖啡店或其他地点是否适合长时间学习；
- 可以推断人格、身份、健康、收入或消费能力；
- 输出是预测概率、已校准置信度、因果增益或跨情境稳定偏好；
- 反通用描述、编辑价值或真实使用行为已经通过验证；
- 已达到 participant-grade research、隐私合规、商业适用性或 production readiness。许可证允许的用途与这些证据结论分开。
