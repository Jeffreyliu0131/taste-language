# Taste Language 体验验收说明

这份说明帮助你体验产品行为，而不是验证研究结论。完成依赖安装后，体验运行时无需账号、模型、API 密钥、后端或外部网络。

## 启动

```bash
npm ci
npm run build
npm run preview:acceptance
```

打开 `http://127.0.0.1:43135/`。服务只绑定本机回环地址。

## 推荐复现路径

| 阶段 | 选择 |
|---|---|
| 前 6 组照片 | A、A、A、B、B、B |
| 3 组未见照片 | B、A、B |
| 四组候选语言 | 任选一项；选 A 可复现默认演示状态 |
| 方向交换 | 任选一项；选 A 可复现默认演示状态 |
| 候选轴 | 先尝试改名为“安静且插座充足”，再接受两个轴 |

## 逐步检查

### 1. 确认情境

- 你应能复述：这里只判断照片中可见的空间画面，不是在挑选真实地点。
- 页面应明确排除噪声、插座、舒适度、拥挤、价格、时段和店规。
- 开始前应能看到完整流程预告，并可选择浅色或深色模式。

### 2. 选择 6 组照片

- A/B 图片应保持等尺寸和等权重，不使用默认单图或强制滑动。
- 在 390px 移动视口中，两张图片和两个选择按钮应同时出现在初始决策视口。
- 应可选择 A、B、同样适合或看不出来，并可撤销上一题。
- 键盘可用 A、B、T、N 快捷键。

### 3. 确认冻结

- 页面应明确列出同时冻结的内容：训练摘要、0-2 个候选轴、3 组未见照片预测和候选位置。
- 信息不足时允许得到 0 个轴或拒答；应用不会强行补写语言。
- 概率始终为 `null`。

### 4. 选择 3 组未见照片

- 页面只收集选择，不显示冻结预测、分值或候选来源。
- 页面应说明这只是客户端界面隐藏，不是真正的研究级盲测。

### 5. 辨认候选语言

- 四组选项应使用相同结构和视觉权重，并提供“都不像”。
- 第二轮只交换一个候选轴的方向，提交前不揭示来源。
- 0 个或 1 个轴时，应明确说明无法组成公平对照，而不是复制假选项。

### 6. 检查证据并编辑

- 每个候选轴应回链前 6 组照片中的支持、同组对照和冲突样本。
- 应支持接受、受控改名、否定、合并和撤销。
- “安静且插座充足”应在输入框下方就地报错，并明确候选轴分值和冻结预测没有改变。

### 7. 查看三份结果

- 未见照片、语言辨认和运行状态应保持分离，不合成为总分。
- 合成演示序列即使三次方向相同，也必须明确不代表准确率、稳定性或产品价值。
- 技术交付明细应默认折叠；未见照片只显示方向、分值或拒答，概率为 `null`。
- JSON 导出应保留三份可解释记录，但不包含图片 bytes、内部候选 ownership ID 或数值概率；概率字段应为 `null`。

## 通过标准

只有以下条件同时成立，体验验收才通过：

- 无需理解代码即可复述产品情境与非目标。
- Desktop light 与 390px mobile dark 均可从头完成，无横向溢出、阶段焦点丢失或 A-first 曝光偏差。
- 冻结后，未见照片选择、语言辨认和编辑都不会改写冻结结果。
- 0/1/2 轴、低差异拒答、越界改名、语言模块失败、图片失败、版本不一致、损坏存储和跨标签清除均 fail closed。
- 结果页诚实、可检查、可导出，且三份记录保持独立。

任一项不成立，本次体验验收即为不通过。

## 重置与导出

- 结果页提供“导出本地记录”和“重新体验”。
- 页首回转箭头可清除当前浏览器里的体验进度。
- 重置只删除应用自己的 localStorage key，不会删除已下载文件、浏览器缓存、操作系统备份或其他应用数据。

## 验收不代表什么

完成这条路径不代表需求、准确率、反通用描述、编辑价值或真实使用行为已经验证，也不代表应用可用于正式研究或 production。详见 [已知限制](KNOWN_LIMITATIONS.md)。
